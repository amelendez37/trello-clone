import React from 'react';
import { shallow } from 'enzyme';
import Board from './board.jsx';

test('Board should render', () => {
//   const component = shallow(<Board />);
//   expect(component.length).toBe(1);
});

// test('Board should render board name', () => {
//   const component = shallow(<Board boardName={'testBoard'}/>);
//   expect(component.find('.board__title').text()).toBe('testBoard');
// });
